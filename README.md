# listd64
Utility for quickly listing directories from D64 files to a single readable text file on your modern PC


The bin directory has the binary for Windows.

The source directory has the C source code that was compiled using TCC (Tiny C Compiler)
It compiles fine with GCC as well but ends up larger.

Documentation is in the listd64.txt file.